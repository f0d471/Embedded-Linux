# 嵌入式 Linux 学习仓（100ASK_IMX6ULL Pro）

目标：学完一整套嵌入式 Linux 从应用到驱动的知识，并整合出一个可运行的完整项目
和一份能当教材用的笔记。

## 一、这个仓和资料仓的关系

配套资料仓（只读，不要往里写东西）：

    E:\Workspace\01_all_series_quickstart-master\01_all_series_quickstart-master

主教材（唯一的文字教程，550 页）：

    嵌入式Linux应用开发完全手册V5.3_IMX6ULL_Pro开发板.pdf

资料仓里没有视频，04/05/06 目录下只有源码和插图。视频在 B 站，PDF 每篇开头都
附了对应的 B 站链接，当辅助材料看。

工作方式固定为：翻 PDF 到某一章 -> 到对应 source 目录找那份代码 -> 读本仓的解读
-> 做本仓的实验。

重要：PDF 目录里标的页码比阅读器里的实际页码小 9。本仓 refs/章节对照表.md
里两个页码都给了，以「PDF 页」列为准直接跳页。

## 二、笔记的定位（本仓的核心）

笔记不是抄书，是三样东西合在一起：

1. 解读：把 PDF 讲得含糊、跳过、或者只给结论的地方补深，打通到硬件和内核层面。
   PDF 负责告诉你怎么做，解读负责告诉你为什么是这样、以及不这样会怎样。

2. 分级实验：每一章配一组实验，按四个强度分级。

       L1 复现   跑通并观察现象。占比要小，只用来建立事实基础。
       L2 改造   给定目标，改现有代码，必须改出预期的可观测变化。
       L3 从零写 不看资料仓源码，自己写一段完整的程序。
       L4 入项目 把这一层能力落进 project/，加出一整层代码。

   L4 是最终目的。每学完一个层级（显示层、输入层、文字层、UI 层...），
   project/ 里就多出可运行的一层。

3. 判据：每个实验都要有一条能判对错的标准。没有判据的实验等于没做。
   判据要能"注错见红"：故意把代码改坏，判据必须失败。

## 三、开发板资源（决定哪些实验能做）

100ASK_IMX6ULL Pro，板载：

    2       用户 LED                    LED 驱动、GPIO、pinctrl 实验
    8 / 9   KEY1 / KEY2                 按键驱动、查询/中断/poll/异步通知实验
    14      板载 HDMI 口                framebuffer 显示，接普通显示器即可
    17      AP3216 光感传感器（I2C）    I2C 应用编程和驱动实验
    31      ICM-20608 六轴传感器        SPI/I2C 进阶
    7       2 路 USB HOST               插 USB 鼠标/键盘，作为 input 设备
    6       USB 转串口                  调试串口
    5       USB OTG                     adb、烧写
    12 / 13 两个百兆网口                网络编程、NFS、网络输入
    33      RTL8723 WiFi/蓝牙
    26 / 27 512MB DDR3L + 4GB eMMC

    15      RGB TFT LCD 接口            没有屏，用 HDMI 替代
            触摸屏                       没有，用 USB 鼠标替代（同样是 /dev/input/eventX）

## 四、环境策略

日常开发在 WSL Ubuntu 里做，只有真正需要上板验证时才连板子。

    第 1 阶段（应用编程）  绝大部分能在 WSL 里用 x86 gcc 跑通逻辑，
                          交叉编译产物攒着，阶段末尾集中上板验一次。
    第 2 阶段（驱动开发）  必须上板。编内核模块要用板子 BSP 里的工具链和
                          内核源码，apt 装的 gcc-arm-linux-gnueabihf 不行。

## 五、本仓结构

    notes/     解读 + 实验 + 判据。最终交付物之一。
      01_应用编程/
      02_驱动开发/
      03_项目/
    labs/      自己动手敲的实验代码，按 章号_名字 建目录。
               不要拷贝资料仓的源码，抄一遍等于没学。
    project/   终点整合项目。逐层长出来。
    refs/      章节对照表、环境记录、板子信息。

## 六、进度

    [ ] 阶段 1  应用编程   PDF 141-300
        [ ] 01 工具链与构建系统（HelloWorld / GCC / Makefile）
        [ ] 02 文件 IO
        [ ] 03 Framebuffer 显示
        [ ] 04 文字显示（ASCII / 中文 / freetype）
        [ ] 05 输入系统
        [ ] 06 网络通信
        [ ] 07 多线程
        [ ] 08 I2C
        [ ] 09 串口
    [ ] 阶段 2  驱动开发   PDF 301-495
    [ ] 阶段 3  项目整合
