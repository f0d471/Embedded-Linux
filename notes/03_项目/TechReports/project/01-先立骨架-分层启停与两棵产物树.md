# 01 先立骨架：分层、启停顺序与两棵产物树

## 一、动机

这个仓要在 100ASK_IMX6ULL Pro 开发板上做出一个电子产品量产工具：
板子接一块屏，屏上画出若干个按钮，点一个按钮就跑一项测试或者烧一次固件，
测试结果回显在屏上。它要读 `/dev/fb0` 出图，读 `/dev/input/eventX` 收鼠标/触摸事件，
要能画中文，要有页面和控件，最后要有一层业务逻辑把这些串起来。

代码不是一次写完的，是跟着课程一章一章长出来的：学完 framebuffer 才有显示层，
学完输入系统才有输入层，学完 freetype 才有字体层。也就是说，
**这棵树在长成之前，要以半成品状态存在十几章的时间。**

半成品状态下有一件事会出问题：加一层的时候要改多少地方。
如果每加一层都要动构建脚本、动主程序、动别的层的代码，那么十几章下来，
每一章的精力都会有一部分花在维护上，而不是花在那一章的知识点上。
更糟的是，早期随手写下的耦合会在后期变成拆不动的东西——
显示层直接被业务层 `open("/dev/fb0")` 之后，就再也换不成别的显示后端了。

所以第一件事不是写显示层，是把"以后每加一层要动哪些地方"这个数字压到最小。
这一章做的就是这件事：先立一个六层都在、但每层都是空壳的骨架，
把加一层的代价固定下来，再往里填。

代价的目标值是三处：新建一个 `.c`、在主程序的层表里加一行、往对应的 `Makefile`
变量里加一个目录名。这一章做完之后实测是两处，第三处被消掉了，见第三节。

这一章不产生任何功能。它的产出是一套约束和一套判据。

### 1.1 先把终点说清楚：最后到底要做出什么

模仿对象是下面这个项目，不是同目录里的 Qt 工业物联网项目：

```
E:\Workspace\01_all_series_quickstart-master\01_all_series_quickstart-master
  \06_实战项目\01_电子产品量产工具
```

它的 `source/02_视频配套源码/` 有 32 份递增源码，最后一份
`32_improve_touch` 是课程从零搭出来的教学版成品。它不是一个普通“点按钮的 GUI”，
而是一台放在产线工位上的板端测试终端：待测板连接显示器和输入设备，操作者从屏幕
选择测试项，程序调用脚本、应用或设备接口完成检测和烧写，再把结果显示回对应按钮。

最终启动后的画面大致是：

```
┌──────────────────────────────────────────────────────────────┐
│  LED     喇叭    录音    KEY1    KEY2    AP3216C              │
│  ICM20608    USB Host    USB Device    串口    Wi-Fi          │
│  网口 0      网口 1      4G      RS485/CAN      烧写           │
│                                                              │
│  红色/未测或失败    绿色/通过    蓝色/进行中，并显示百分比          │
└──────────────────────────────────────────────────────────────┘
```

按钮不是在 C 代码里一项一项写死。配置文件至少描述：

```
名称        是否允许人工点击        点击或状态变化时调用的命令
led         1                       led_test.sh
ap3216c     1                       ap3216_test
burn        1                       burn.sh
```

程序读取配置后按屏幕尺寸自动排版。输入有两类：

- 本地输入：本仓使用 USB 鼠标，从 `/dev/input/eventX` 读坐标变化和按键；
  参考项目用触摸屏，两者最后都要变成同一种 `InputEvent`。
- 外部结果：测试程序或另一台机器通过网络发来 `wifi ok`、`burn 70`、
  `serial err` 这类状态，程序找到同名测试项并刷新显示。

本仓完成后的最小闭环是：

```
启动 product_tool
    -> 读配置，生成测试项和按钮
    -> 在 HDMI 对应的 /dev/fb0 上画出界面
    -> USB 鼠标点一个测试项
    -> business 启动对应脚本、应用或设备测试
    -> 收到 running / progress / pass / fail
    -> 只刷新对应按钮，并把过程写进日志
    -> 继续等待下一次输入
```

所以“项目完成”不等于六行 `init OK`，也不等于屏幕能画一个方块。至少要完成
**配置驱动界面、统一输入事件、执行一个真实测试项、结果回显**这四件事，才形成闭环。

### 1.2 本仓要模仿什么，哪些地方不照抄

模仿的是三个核心能力：

1. **设备可替换。** display、input、font 都是“统一接口 + 具体后端 + 注册表”。
   上层选中一个后端后只用统一接口，不知道底下是 framebuffer、鼠标还是 FreeType。
2. **测试项由配置生成。** 换一批产品或测试项，优先改配置和业务适配器，
   不去重写页面布局。
3. **事件驱动。** 本地操作和外部测试结果都进入统一事件流，页面根据事件局部刷新。

参考源码只是教学起点，不是逐行复制的规格。它的最终版仍有几处边界混在一起：
`page/main_page.c` 同时解析业务状态、调用 `system()`、改按钮颜色；
输入条件变量被唤醒后没有用 `while` 重查队列；配置解析没有跳过空行，也没有检查
30 项数组的边界。本仓已经把后三项记入路线图，施工时要用判据把它们挡住。

本仓采用下面的职责划分：**page 决定页面上有什么以及事件落在哪个控件，business
决定这个测试项要做什么以及结果是什么。** 命令执行、驱动访问和通过/失败判定不写进
按钮回调。这样以后把 shell 脚本换成直接读驱动，不需要改 UI 和 page。

### 1.3 终态架构：六层，加两个支撑模块

六层不是六个首尾相接的函数。更准确的依赖图是：

```
                           main.c
                    只管初始化、运行、退出
                              |
                              v
                         business
              测试项状态机、执行测试、解释结果
                 /             |              \
                v              v               v
             config          input          page
          测试项定义      统一输入事件     页面与事件路由
                                              |
                                              v
                                              ui
                                         Button 等控件
                                          /          \
                                         v            v
                                       font        display
                                    字符到字模      像素到屏幕

    common：错误码、日志等公共设施；各层都能用，但不能反过来依赖业务层。
```

图里的箭头表示**谁调用谁**，只能由上往下。运行时看起来像“鼠标事件从 input 往上
跑到 business”，实际调用仍是 business 向下调用 `input_get_event()`，事件作为返回值
带回来；下层不会包含上层头文件，也不会反过来调用 business。

启动顺序仍采用本章骨架的自底向上：

```
display -> input -> font -> ui -> page -> config -> business
```

其中六个核心层是 display / input / font / ui / page / business。`config` 是业务的支撑
模块，进入整合阶段后可以作为一个有生命周期的启动项放在 page 与 business 之间；
`common` 在分层之外，最先可用、最后失效。依赖关系而不是目录的上下位置决定顺序：
ui 要画字和图，所以 font、display 先好；business 要读配置和操作页面，所以最后启动。

主循环最终应由顶层 business 驱动，形状如下：

```c
while (!quit) {
    input_get_event(&event);                    /* 向下取一个统一事件 */
    action = page_resolve_event(&event);        /* 坐标/名称 -> 测试项 ID */
    business_handle(action, &event);            /* 开始测试或接收测试结果 */
    page_update_item(action.item_id, action.status, action.progress);
}
```

这段是目标形状，不是当前已经存在的 API。当前 `main_loop()` 里“交给 page 层跑页面
事件循环”的 TODO 到项目整合时应改成调用 `business_run()`；否则顶层 business 只有
名字，没有真正承担业务编排。

### 1.4 每层到底负责什么

| 层/模块 | 自己拥有的东西 | 对上层提供什么 | 明确不负责什么 |
|---|---|---|---|
| `display` | 显示后端注册表、默认后端、分辨率/bpp、显示缓冲；framebuffer 后端拥有 `/dev/fb0`、`ioctl`、`mmap` 得到的资源 | 选后端、取画布、画点/区域、刷新区域 | 按钮、测试项、鼠标、命令执行 |
| `input` | 输入后端注册表；鼠标/网络等设备；每个输入源的线程；受互斥锁保护的有界队列 | 把不同来源统一成带类型、坐标、按键/压力、文本和时间的 `InputEvent`；阻塞取事件 | 判断点中了哪个按钮、执行测试、改屏幕颜色 |
| `font` | 字体后端注册表；FreeType face、字号；可选的 ASCII/HZK 后端 | 给一个字符或字符串，返回字模、步进和外框尺寸 | 决定文字放在哪个按钮、直接操作 `/dev/fb0` |
| `ui` | `Button` 等控件的几何区域、视觉状态、绘制与命中规则 | 初始化控件、绘制、改变状态、判断一个位置是否命中 | 读配置、打开输入设备、执行 shell 命令、判断硬件是否合格 |
| `page` | 页面注册表；主页面的控件集合和布局；控件 ID 与测试项 ID 的对应关系 | 创建/显示页面，把输入事件解析成页面动作，按业务给出的状态刷新某个测试项 | 直接读驱动、解析测试输出、决定 pass/fail、调用 `system()` |
| `business` | 测试项运行状态；测试执行器/适配器；运行、通过、失败、进度的转换规则 | 启动主循环，接收页面动作，执行 LED/I2C/串口/网络/烧写等测试，把结果交给 page | 像素格式、字模、鼠标坐标换算、页面排版 |
| `config` | 配置项数组或动态表；名称、是否可操作、执行器/参数等字段 | 加载并校验配置，按 ID 或名称查询；拒绝空项、重复项、超限项和非法命令 | 执行命令、创建 Button、访问硬件 |
| `common` | 统一错误码、日志、通用小工具 | 所有层都能使用的稳定基础设施 | 保存某一层的私有状态，形成“万能工具箱”式业务依赖 |
| `main.c` | 各启动项的顺序和已成功数量 | 初始化、失败回滚、进入 `business_run()`、反序退出 | 打开任何具体设备、画控件、解析配置或实现测试项 |

display、input、font 的共同结构是：

```
manager（统一接口和注册链表）
    |
    +-- backend A（framebuffer / mouse / freetype）
    +-- backend B（以后增加，不改上层）
```

ui 和 page 也可以注册控件类型与页面，但它们解决的是界面组合问题；business 的扩展
单位则是“测试项适配器”。新增 AP3216C 测试时，应增加一个 business 适配器并在配置
里加一项，不应该让 display、input、font 跟着改。

### 1.5 两条运行链路，先在脑中固定下来

本地点击启动测试：

```
/dev/input/eventX
  -> mouse 后端把相对位移累加并限制在屏幕内
  -> input manager 送出统一 InputEvent
  -> page 用坐标找到按钮和测试项 ID
  -> business 把该项设为 RUNNING，启动脚本/应用/驱动操作
  -> page 把按钮画成进行中
```

测试结果回显：

```
测试执行器返回值 / 驱动数据 / 网络消息 "wifi ok" 或 "burn 70"
  -> business 校验测试项名称和状态
  -> 更新 NOT_TESTED / RUNNING / PASS / FAIL 与 0..100 进度
  -> page 找到对应控件
  -> ui 重新画按钮底色和文字
  -> font 生成字模
  -> display 只刷新这个区域
```

这两条链路故意把“测试结果是什么意思”留在 business，把“怎么画出来”留在 page/ui。
如果某个实现需要同时包含 `linux/fb.h`、`linux/input.h` 和 `stdlib.h` 里的 `system()`，
基本可以判定边界又混回去了。

### 1.6 跟着课程施工：每学完一章，项目必须长出什么

下面的“完成现象”是该章 L4 入项目的最低交付，不是读完笔记就自动算完成。
每一行都要同时有代码、单层验证、`project/check.sh` 回归和对应工程文档。

| 本仓课程              | 对应参考源码                   | 要落进 `project/` 的东西                                                                                                        | 这一章结束时必须看见什么                                                     |
| ----------------- | ------------------------ | ------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------- |
| 01 工具链与构建         | 参考项目没有，属于本仓先补的地基         | 当前六层空壳、统一错误码和日志、层表、失败回滚、非递归 Makefile、x86/ARM 两棵产物树                                                                        | 两种架构都能编译；正常启停严格正序/反序；任一层失败只回滚成功层                                 |
| 02 文件 IO          | 无直接对应                    | `common/log_redirect()`；`LOG_FILE` 在所有层初始化前接管 stderr；追加写和失败返回判据                                                           | 不改六层代码即可把完整生命周期日志写入文件，多次运行继续追加                                   |
| 03 Framebuffer 显示 | 01～04                    | `DispBuffer`、`DispOps`/注册选择接口、`display/framebuffer.c`；打开 `/dev/fb0`，读取真实分辨率和 bpp，`mmap` 显存；`unittest/disp_test.c` 和单测构建目标 | HDMI 屏能依次显示几种纯色和指定矩形；退出后映射和 fd 都释放；上层不出现 `/dev/fb0`              |
| 04 文字显示           | 13～17                    | `FontOps` 注册选择接口、FreeType 后端和字体资源；字模、字符串外框、字号接口；`font_test`；按实际工具链补 include 与链接选项                                         | 在 framebuffer 上显示 ASCII 和中文，文字外框、基线、裁剪可观察；切字号不改 display          |
| 05 输入系统           | 05～07、10、12；触摸实现不照抄      | `InputEvent`、`InputDevice`、注册接口、`input/mouse.c`；直接解析 `struct input_event` 的 `EV_REL`/`EV_KEY`，把相对位移累加成屏幕绝对坐标；`input_test` | 移动 USB 鼠标时持续打印被限制在屏幕范围内的坐标，按下/松开各产生一次明确事件                        |
| 06 网络通信           | 08～09                    | `input/netinput.c`，用 UDP 接收带测试项名称和状态的消息，转换为同一个 `InputEvent`；网络输入单测客户端                                                     | 本地鼠标与 UDP 消息走同一个 `input_get_event()`；发送 `wifi ok` 能得到结构化事件       |
| 07 多线程            | 11                       | 每个输入后端一个接收线程；有界环形队列、互斥锁、条件变量和退出协议；等待条件必须用 `while` 重查；按需要增加 business 工作线程，避免长测试卡死事件处理                                      | 鼠标和网络事件交错进入仍不丢结构、不读坏数据；无事件不忙等；退出能唤醒并回收线程                         |
| 08 I2C            | 参考 GUI 只提供测试项名称，具体能力由本仓补 | AP3216C 业务适配器。先用用户态 `/dev/i2c-*` 建立基线；按当前驱动路线完成后改接 `/dev/ap3216c`，接口保持不变；配置加入 `ap3216c`                                   | 遮挡/放开传感器得到可解释的数值变化，business 能把读失败和数值结果转换为状态并显示                   |
| 09 串口             | 参考项目通过外部程序/脚本执行          | 串口业务适配器：配置波特率和帧格式，执行回环或与治具通信，处理部分读写、超时和校验；配置加入 `serial`                                                                   | 回环正确显示 PASS，拔线或超时显示 FAIL，失败原因进入日志且页面仍能继续响应                       |
| 项目整合              | 18～28                    | `Button`、UI 单测；页面注册与主页面；健壮的 config；`business_run()` 和测试项注册；至少把 LED、AP3216C、串口中的一个真实测试做成点击到回显闭环                            | 改配置即可改变按钮数量/名称；点击真实测试项经历 RUNNING 后落到 PASS/FAIL；不改 page 就能新增测试适配器 |
| 收尾打磨              | 29～32                    | 自适应按钮文字和局部刷新；稳定的层间 API；进度事件；鼠标命中/抖动处理；剩余板载测试和烧写脚本按实际资源逐项接入                                                                | 连续操作不越界、不死锁；错误配置有明确报错；测试失败不拖垮主循环；板上完整验收脚本全绿                      |

参考项目的 32 步可以压成七段来查：`01～04 display`、`05～12 input`、
`13～17 font`、`18～20 ui`、`21～23 page`、`24～28 business/config`、
`29～32 改进`。本仓按课程章节重排了施工顺序，所以不追求目录编号同步，只要求
每个接口和现象能在上表找到来处。

每次准备把一行标成完成，都问五个问题：

1. 新能力落在哪一层？有没有让上层包含具体设备头文件？
2. 本层的资源由谁申请、失败时回滚哪些、退出时按什么顺序释放？
3. 有没有一个只测本层的最小程序，而不是只能启动整个成品才能试？
4. 正常现象是什么，故意改坏哪一处时判据必须变红？
5. x86 能测哪些纯逻辑，ARM 板上还欠哪条硬件证据？

五个问题都能回答，这一章才算真的给项目加了一块，而不是把课程示例复制进目录。

## 二、为什么是这几步

这个工作点拆成四步，因为"加一层的代价"由四件互相独立的事决定。

**第一步，定分层与调用方向。** 决定一共有哪几层、谁能调谁。
这一步不定，后面三步都无从谈起：不知道有几层，就不知道构建系统要收几个目录，
也不知道主程序要按什么顺序拉起它们。

**第二步，定启停顺序与失败回滚。** 有了层次就有了依赖，有依赖就有先后。
这一步单独成立，是因为它有一个不显然的部分：初始化到一半失败时该退出哪几层。
这部分写错不会立刻出现象，会在某一层第一次初始化失败的那天才炸。

**第三步，定构建系统。** 前两步是源码组织，这一步是从源码到产物。
它单独成立的理由是它要同时满足两个互相拉扯的要求：
本机 x86 编出来在 WSL 里调逻辑，交叉编出来拿去上板，两份产物要能并存。

**第四步，把前三步变成判据。** 前三步都是"约定"，约定不会自己成立。
没有判据的约定会在三章之后被自己破坏掉，而且破坏的时候没有任何提示。

少一步都不成立：不做第一步会得到一个平铺的目录；不做第二步会在退出路径上留
一个只在失败时触发的洞；不做第三步会在每次上板前手工改编译命令；
不做第四步，前三步写在文档里的东西和代码里实际的东西会各走各的。

## 三、每一步做了什么

### 3.1 分层与调用方向

六层，自底向上：

```
    +-------------------------------------------------+
    |  business    业务逻辑, 决定点了按钮之后干什么   |  高层
    +-------------------------------------------------+
    |  page        页面调度, 一个页面是一屏内容       |
    +-------------------------------------------------+
    |  ui          控件, 目前规划里只有按钮           |
    +-------------------------------------------------+
    |  font        取字模: ASCII 点阵 / HZK16 / freetype |
    +-------------------------------------------------+
    |  input       事件源: /dev/input/eventX 等        |
    +-------------------------------------------------+
    |  display     出图: /dev/fb0 等                   |  底层
    +-------------------------------------------------+

    调用方向只能向下。
```

这个分法直接取自课程配套项目 `06_实战项目/01_电子产品量产工具`，
它的 32 个演进步骤就是按 display / input / font / ui / page / business 这个顺序推的，
目录名也是这六个。照搬它是为了让本仓的每一层都能和那边的某一步对上，
学到哪一步就能拿那边的代码做对照。差异之处记在
[`../../Todo/项目路线图-对齐电子产品量产工具.md`](../../Todo/项目路线图-对齐电子产品量产工具.md)。

调用方向靠两条落地：

一是每层只暴露一个头文件，头文件里只有函数声明，没有结构体定义。
本层的内部状态别的层看不见，才谈得上以后整层换掉。
`display/disp_manager.h` 现在只有两行声明，就是这个约定的形状。

二是 `main.c` 站在这栋楼外面。它不 `open` 任何设备节点，不认识 `/dev` 下的任何路径，
只认每层的 `init` / `exit` 两个函数指针。这一条写进了 `main.c` 的文件头注释。

### 3.2 启停顺序与失败回滚

初始化自底向上，退出自顶向下：

```
    init 正序                         exit 反序

    1 --> display   init OK           display   exit OK <-- 6
    2 --> input     init OK           input     exit OK <-- 5
    3 --> font      init OK           font      exit OK <-- 4
    4 --> ui        init OK           ui        exit OK <-- 3
    5 --> page      init OK           page      exit OK <-- 2
    6 --> business  init OK           business  exit OK <-- 1
```

反序的理由是资源的生命周期必须包含使用者的生命周期：
`business` 还在用 `page` 的数据时，`page` 不能先关。这是栈式管理，
和 C++ 的构造析构顺序、Linux 内核模块的 `module_init` / `module_exit` 是同一件事。

不显然的是失败路径。初始化到第三层失败时，已经成功的是前两层，
第三层自己没成功，不能对它调 `exit`：

```
    display OK --> input OK --> font FAIL
                                  |
                                  v
                        只回滚前两层, 顺序 input -> display
                        font 没 init 成功, 不能 exit
```

实现上把"退出前 n 层"抽成一个函数，正常退出传层数，失败回滚传已成功的层数，
两条路径共用一份代码，不会出现只改了一边的情况。

顺序本身写成一张表，加一层就是加一行。表里同时存着层名，
日志和判据都从这个层名来，不另写一份字符串。

### 3.3 构建：一棵源码树，两棵产物树

```
    源码树                          build/x86/                 build/arm/
    ------------------------        --------------------       -------------------
    main.c                 --->     main.o   main.d            main.o   main.d
    common.c               --->     common.o common.d          common.o common.d
    display/disp_manager.c --->     display/disp_manager.o     display/...
    ...                                  |                          |
                                         v                          v
                                  product_tool (x86-64)      product_tool (ARM)

    make                              -> ARCH=x86
    make CROSS=arm-linux-gnueabihf-   -> ARCH=arm
```

三个决定：

**产物按架构分目录，不放在源码旁边。** 两棵产物树互不覆盖，
x86 版在 WSL 里调逻辑、ARM 版拿去上板，中间不用 `make clean`。
副作用是源码树永远干净，`.gitignore` 里一条 `build/` 就够。

**架构名从工具链前缀算出来，不手写。** `arm-linux-gnueabihf-` 按 `-` 切开取第一段
得到 `arm`，前缀为空则是 `x86`。手写会出现前缀和目录名对不上的构型，
而那种构型编出来的东西看不出错。

**头文件依赖交给编译器生成。** 编译时加 `-MMD -MP`，
编译器顺带吐出一份"这个 `.o` 依赖哪些 `.h`"的规则文件，`make` 再把它们读回来。
不做这一步的后果不是慢，是错：改了一个头文件里的结构体定义，
只有一部分 `.c` 重编，两边对同一块内存的理解不一致，编译零警告，运行行为诡异。

源文件用 `$(wildcard)` 按目录收集。代价是删掉一个 `.c` 而忘了删它的引用时，
报错发生在链接期而不是编译期；收益是加一层时不必回来改文件清单。
第二步说的"加一层的代价三处"，被这一条消掉了一处，剩两处：
新建 `.c`，以及在 `main.c` 的层表里加一行。

这里有一个和课程明确分歧的地方。课程的第 4_3-6、4_3-7 两节给了一份"通用 Makefile"，
形状是仿 Linux 内核的递归构建：每个目录一份 `Makefile` 声明 `obj-y`，
顶层 `Makefile.build` 递归进各子目录，每个目录先把自己的 `.o` 用 `ld -r` 并成一个
`built-in.o`，再层层并上去，最后链接。本仓的 `project/` 没有采用它，理由是
这份骨架只有六个平铺的子目录，递归构建带来的能力（任意深的目录树、
按目录设编译选项、按单个文件设编译选项）当前一个都用不上，
而它的代价是构建过程分散在七个文件里，出问题时要跨文件追。

这不等于那套东西不用学。它的价值在第 2 阶段：Linux 内核和 U-Boot 的构建就是这套，
读内核 Makefile 时躲不开。所以它被放进了 `notes/01` 的动手部分，
作为一个从零复现的实验，而不是作为 `project/` 的构建系统。

### 3.4 把前三步变成判据

判据落在 `project/check.sh`，`bash check.sh` 跑一次给出 PASS / FAIL / SKIP 计数。

两条原则：

**判据做成计数型或序列型，不做"某条输出里有某个词"型。**
"输出里有 display" 这种判据，在六层只剩一层能跑的时候照样绿。
现在数的是 `init OK` 出现几行、这六行按什么顺序出现，少一层立刻从 6 变 5。

**每条正判据后面跟一次注错见红。** 故意把代码或 Makefile 改坏，
判据必须变红。注错不红的判据等于没有判据——它当时是绿的，
但你不知道它是因为代码对而绿，还是因为它根本量不到东西而绿。

破坏性操作全部在临时副本里做，`mktemp -d` 出一个目录，把整个工程复制进去，
`trap` 挂在 `EXIT` 上保证退出时清掉。工作区一个字节不动。

四组注错分别打在四个位置：删掉层表里的一行、把 `wildcard` 换成写死的文件清单、
去掉 `-MMD -MP`、以及第二组里的 `make clean` 与 `distclean` 的区别。
每一处对应上面三步里的一条约定。

## 四、遇到的问题

骨架是一次写成的，没有留下调试记录，所以本节记的不是事故，
是两个"换一种写法会怎样"的当场实测。两段输出都是在本机跑出来的，
命令附在各自段落里，可以复现。

### 4.1 判据的字段为什么锚行尾而不是行首

日志宏会在每行前面加级别标签和文件行号，所以层名不在行首：

```
    [I] display/disp_manager.c:14 display init OK
     ^                             ^
     $1                            $(NF-2)
```

同一份输出，两种取法的实测结果：

```
    $ echo "$out" | grep 'init OK' | awk '{print $1}'       | tr '\n' ' '
    [I] [I] [I] [I] [I] [I]

    $ echo "$out" | grep 'init OK' | awk '{print $(NF-2)}'  | tr '\n' ' '
    display input font ui page business
```

取 `$1` 得到的是六个相同的值。这样的判据既区分不出层，也区分不出顺序，
它的输出与代码是否正确无关——它是一条恒定输出的判据。

`awk` 里 `NF` 是本行的字段总数，`$(NF-2)` 是倒数第三个字段。
行尾三个字段固定是 `<层名> init OK`，所以锚行尾。
额外的好处是日志前缀以后再变（加时间戳、加线程号），这条判据不用跟着改。

### 4.2 "词存在型"判据在少了一层的时候仍然是绿的

把 `main.c` 层表里 `business` 那一行删掉，六层变五层，然后用三种判据量同一份输出：

```
    --- 删掉 business 层之后的输出行数: 11
    --- 词存在型  输出里有 "init OK"   : PASS
    --- 词存在型  输出里有 "display"   : PASS
    --- 计数型    init OK 恰好 6 行    : FAIL
```

前两条在功能已经缺了六分之一的情况下照样绿。这就是 `check.sh` 开头那条原则
"判据尽量做成计数型或序列型"的来历：词存在型判据的通过条件太宽，
它在代码正确时是绿的，在代码错成这样时也是绿的，所以它不携带信息。

这一段同时说明了注错见红这个动作本身在检验什么：
它检验的不是代码，是判据。判据注错之后仍然是绿的，就说明它是上面那种。

## 五、结果

本机 WSL2 Ubuntu 26.04、gcc 15.2.0、arm-linux-gnueabihf-gcc 15.2.0，
`bash project/check.sh` 实测：

```
    PASS=18  FAIL=0  SKIP=0
```

十八条的构成：

| 组 | 条数 | 量的是什么 |
|---|---:|---|
| [1] 构建 + 启停顺序 | 5 | 退出码、init 行数、exit 行数、init 顺序、exit 反序 |
| [1r] 注错：删掉层表里 business | 2 | init 行数由 6 变 5（红）、其余五层照常启停 |
| [2] 交叉编译 | 5 | ARM 产物 Machine、x86 产物 Machine、两份并存、clean 只清一边、distclean 清干净 |
| [3] 新增 .c 不改 Makefile | 1 | 新文件的 .o 出现 |
| [3r] 注错：wildcard 换成写死清单 | 1 | 那个 .o 消失（红） |
| [4] 头文件自动依赖 | 1 | touch include/common.h 后重编 8 个文件 |
| [4r] 注错：去掉 -MMD -MP | 1 | 重编数变成 0（红） |
| [5] 日志整体关闭 | 2 | LOG_LEVEL=0 时输出 0 行、退出码仍为 0 |

其中"重编 8 个文件"这个数不是写死的，是 `check.sh` 自己数出来的源文件个数，
所以以后加层加文件时这条判据不用跟着改。

代码规模：8 个 `.c`、7 个 `.h`、`Makefile` 70 行、`check.sh` 106 行。
两份产物都能编出来，`readelf -h` 分别显示 `ARM` 和 `Advanced Micro Devices X86-64`。

**没有上板。** ARM 产物只验到"是 ARM 格式且能编出来"，
没有拷到板子上跑过，所以"六层能在板子上按顺序起来"这件事目前没有证据。
板子已到货，这条补在下一章。

## 六、还欠什么

三条，都在 [`../../Todo/项目路线图-对齐电子产品量产工具.md`](../../Todo/项目路线图-对齐电子产品量产工具.md)：

- ARM 产物没上过板。第 01 章的上板实操做完之后补一次，
  判据是板上串口打出的六行 `init OK` 与本机顺序一致。
- 六层现在是空壳，对外接口只有 `init` / `exit`。
  课程配套项目的核心形状是"结构体加函数指针加链表注册"，
  显示层做到第 03 章时要把这个形状引进来，那时 `disp_manager.h` 会大改。
- 通用 Makefile 那套递归构建当前没进 `project/`，只在 `notes/01` 里作为实验复现。
  如果后面目录层次真的变深，再回来重新评估。
