# project 逐行精读

对应 `project/` 下的全部源文件，共 5 篇。

这棵树目前是一个骨架：六层都在，每层都是空壳，没有任何一行代码碰过硬件。
它现在的内容是"加一层的时候要动哪些地方"这个约定、守住这个约定的判据，
以及一件真做事的小功能：把日志整条接到文件上（第 02 章加的）。
为什么是这个形状，见 [`../../TechReports/project/`](../../TechReports/project/) 的[第 01 章](../../TechReports/project/01-先立骨架-分层启停与两棵产物树.md)（骨架）和[第 02 章](../../TechReports/project/02-日志落文件-让现有LOG自动写入文件.md)（日志落文件）。

## 篇目

按依赖关系排列，从下往上读。

| 篇                            | 对应文件                              |          行数 | 这个文件做什么                      |
| ---------------------------- | --------------------------------- | ----------: | ---------------------------- |
| [common](common-逐行精读.md)     | `include/common.h` + `common.c`   |     72 + 60 | 全项目公共约定：错误码、分级日志、数组长度宏、日志重定向 |
| [层管理器空壳](层管理器空壳-逐行精读.md)     | `display/` 等六层的 `*_manager.{c,h}` | 27 + 13 各六份 | 一层的最小形状：两个函数，一个幂等标志          |
| [main](main-逐行精读.md)         | `main.c`                          |         107 | 层表驱动的启停，失败只回滚已成功的层           |
| [Makefile](Makefile-逐行精读.md) | `Makefile`                        |          70 | 一棵源码树编出两棵产物树                 |
| [check.sh](check-逐行精读.md)    | `check.sh`                        |         148 | 26 条判据，每组正判据配一次注错见红          |

## 三个贯穿全目录的约定

**调用方向只能向下，`main.c` 站在楼外面。**
六层自底向上是 display、input、font、ui、page、business，
上层可以调下层，反过来不行。`main.c` 不 `open` 任何设备节点，
不认识 `/dev` 下的任何路径，只认每层的 `init` / `exit` 两个函数指针。

**层的对外接口只有函数声明，没有结构体定义。**
每层一个头文件，里面现在只有两行。本层的内部状态别的层看不见，
以后才谈得上整层换掉（HDMI 换成 LCD、USB 鼠标换成触摸屏）。

**加一层只动两处。** 新建一个 `.c`，在 `main.c` 的层表里加一行。
`Makefile` 不用动，因为源文件是 `$(wildcard)` 按目录收的；
判据也不用动，因为它数的是自己算出来的源文件个数。

## 已知的空实现

读代码时会遇到几处结构完整但当前不干活的地方，列在这里免得反复追。

| 位置 | 情况 |
|---|---|
| 六层的 `*_init()` 函数体 | 只置一个标志、打一行日志，`TODO` 注释里写着哪一章来填 |
| `main.c` 的 `main_loop()` | 打一行日志就返回，页面事件循环等 page 层有实现之后再接 |
| `Makefile` 的 `LDFLAGS` / `LDLIBS` | 空。接 freetype 和 pthread 时才有内容 |
| `log_redirect` 的日志轮转 | 没有。文件会一直长，等 business 层按测试项写日志时再解决 |

## 目标形状

这棵树对标的是课程配套项目 `06_实战项目/01_电子产品量产工具`，
六个层名和调用方向都取自那边。那边的核心形状是"结构体加函数指针加链表注册"，
本仓现在还没引进来。两边的逐项差异与补齐顺序见
[`../../Todo/项目路线图-对齐电子产品量产工具.md`](../../Todo/项目路线图-对齐电子产品量产工具.md)。
