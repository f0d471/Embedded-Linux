# 00 基础通用知识

## 这个文件夹解决什么问题

主线章节（01、02、03...）讲的是"这一章的事"，遇到通用名词只能一笔带过，
不然主线就断了。但一笔带过的结果是：你看到 `s0`、`sysroot`、`PLT` 这种词，
知道它在句子里的位置，却不知道它是什么。

所以约定：

    主线章节里出现一个名词，如果它
        不属于本章主线            例如 ARM 寄存器编号不属于"工具链"这一章
        且以后还会反复出现        例如 ELF、系统调用、动态库
    就在这里单开一篇，主线里只留一句话加一个跳转。

用到就加，不预先写全。没被任何一章引用过的篇章不要写。

## 体例

每篇都按同一个结构：

    1. 一句话说它是什么
    2. 和你已知的东西对照 —— 这一节必须有。
       你有 ysyx（RISC-V）和数字 IC 基础，所以每个概念都从 RISC-V 那边搭桥过来，
       只讲"ARM/Linux 这边不一样在哪"，不从零讲什么是寄存器。
    3. 展开讲，配 ASCII 图
    4. 怎么亲手验证（命令 + 每个参数在干什么）
    5. 被哪些章节用到

## 索引

| 篇 | 内容 | 被谁用到 |
|---|---|---|
| [01 ELF 与程序加载](01_ELF与程序加载.md) | ELF 头、section 与 segment 的区别、execve 到 main 的完整路径 | 01 章 3、4 节 |
| [02 ARM 寄存器与调用约定](02_ARM寄存器与调用约定.md) | r0-r15、s0-s31、AAPCS 传参、Thumb-2，全程对照 RISC-V | 01 章 2.1 |
| [03 ABI 与 sysroot](03_ABI与sysroot.md) | ABI 到底包含什么、sysroot 是什么、-I/-L/-l 搜索顺序 | 01 章 2 节 |
| [04 静态库动态库与符号](04_静态库动态库与符号.md) | 符号表 U/T/W、.a 与 .so、PLT/GOT、重定位 | 01 章 4 节 |
| [05 系统调用与用户内核态](05_系统调用与用户内核态.md) | 特权级、svc/ecall、syscall 号、printf 到内核的完整路径 | 01 章 5 节 |

## 待写（等主线用到了再写，别提前写）

    进程与内存布局        02 章文件 IO 之后
    文件描述符与 VFS      02 章
    字符设备与设备节点    03 章 framebuffer
    中断与信号            05 章输入系统 / 第 2 阶段驱动
    设备树                第 2 阶段
