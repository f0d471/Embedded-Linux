# 第 0 阶段：环境与工具

## 实验 0-1：同一份 hello.c，编译两次

对应 PDF 第 132-133 页，源码 $Q\04_...\source\01_hello\hello.c

### 操作记录

    sudo apt install -y gcc-arm-linux-gnueabihf file
    gcc                     -o hello_x86 hello.c
    arm-linux-gnueabihf-gcc -o hello_arm hello.c
    file hello_x86
    file hello_arm
    ./hello_x86 weidongshan
    ./hello_arm weidongshan

file 的输出：

    hello_x86: (贴在这里)
    hello_arm: (贴在这里)

运行 hello_arm 的报错：

    (贴在这里)

### 要回答的问题

1. file 的两行输出里，哪几个字段不一样？各是什么含义？

2. hello_arm 为什么不能在本机跑？内核是在哪一步、依据什么发现它跑不了的？

3. hello_x86 里的 printf 最终是谁在干活？编译进可执行文件里了，还是运行时才找到的？
   （用 ldd hello_x86 看）

4. 交叉编译器为什么叫 arm-linux-gnueabihf-gcc？这四段名字分别什么意思？

5. 编译 ARM 程序时用的 stdio.h 和 C 库，是 Ubuntu 上那份吗？在哪里？
   （用 arm-linux-gnueabihf-gcc -print-search-dirs 和 -v 看）

## 待办

    [ ] 实验 0-1 做完并答完 5 个问题
    [ ] 开发板串口连通，能进系统
    [ ] 确认板子型号（Pro / mini emmc），锁定用哪本 PDF
    [ ] 下载板子 BSP，装官方交叉工具链（编驱动必须用它，apt 那个不行）
    [ ] 第 1 个 APP 跑到板子上（PDF 90-92 页）
